package com.assignment.sapient;

public class AssignmentTestCase {

    /*
     * @Test public void test_inputFileFound() throws FileNotFoundException { String
     * inputFilePath = Assignment.inputFilePath; File file = new
     * File(inputFilePath); FileReader fr = new FileReader(file); assertNotNull(fr);
     * }
     *
     * @Test public void test_countryDetails() throws IOException {
     * List<CountryDetails> list = Assignment.readFile(); assertNotNull(list);
     * assertEquals("M", list.get(0).gender); assertEquals(expected, actual);
     *
     * }
     */

}
